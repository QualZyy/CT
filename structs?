#include <stdio.h>

//Person struct
struct person {
    char _name[40];
    long _birthdate;
    int _infected;      //TRUE OR FALSE
    long int _date;
    double _time;
};


//Room visits struct
struct rooms {
    struct person _visitors[900];
    long int _when[900];    
};

struct person personRegister[1000]; //Register of all registered people

int main() {
    int exit = 0;
    while(!exit) {
        printf("")

        int option;
        scanf("%i", &option);

        switch(option) {
            case 1: //Register new person
                printf("Enter name");
                char name[40];
                scanf("%s", &name);
                printf("Enter date of birth YYYYMMDD");
                long birthDate;
                scanf("%li", &birthDate);
                
                if(/*Run function that returns 1 or 0*/) {
                    printf("Person was successfully added!");
                } else {
                    printf("An error occured");
                }
            break;

            case 2: //Delete person

            break;

            case 3: //Register room

            break;

            case 4: //Delete room

            break;

            case 5: //Register contamination

            break;

            case 6: //Register a visit

            break;

            case 7: //Write from file

            break;

            case 8: //Read to file

            break;

            case 9:
                exit = 1;
            break;

            case 69:
                printf("Nice");
            break;

            default:
                printf("wtf");
            break;
        }
    }
    return 0;
}

int registerPerson(char name[40], long dateOfBirth) {
    if () //at 
}

int readFromFile() {
    for/while{}
        registerPerson
}
